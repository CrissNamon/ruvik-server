language: java
jdk:
  - openjdk11

script: ./mvnw test -B

notifications:
  email:
    recipients:
      - danilarassokhin@gmail.com
    on_success: never
    on_failure: always

services:
  - postgresql

before_install:
  - psql -c 'create database ruvik;' -U postgres